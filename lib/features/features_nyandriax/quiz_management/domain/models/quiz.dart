class Quiz {
  String description;
  String name;
  int difficulty;
  int categoryId;

  Quiz({
    required this.description,
    required this.name,
    required this.difficulty,
    required this.categoryId,
  });
}
